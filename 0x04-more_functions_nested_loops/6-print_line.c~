#include "main.h"

/**
 *print_line - prints a line
 *
 *@n: length of line
 */

void print_line(int n)
{
	int j = 0;

	if (n > 0)
	{
		for (;j <= n; j++)
		{
			_putchar('_');
		}
	}
	else
	{
		_putchar('\n');
	}
	_putchar('\n');
}
