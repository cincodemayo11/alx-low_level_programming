#include <stdio.h>

/**
 * add - Adds two integers and returns
 * the result.
 *
 * @i: number one.
 * @j: number two.
 *
 * Return: Add of number one and number two.
 */

int add(int i, int j)
{
	int result;

	result = i + j;

	return (result);
}
